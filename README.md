# Typescript with Knockout and require.js

## Start

With TypeScript and Typings installed, and this folder opened in terminal:

- `tsc` to transpile src/greeter.ts once
- `tsc -w` to watch for changes
- `open index.html`